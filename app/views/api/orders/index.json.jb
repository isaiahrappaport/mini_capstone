@orders.map do |order|
  {
    user_id: order.user_id,
    product_id: order.product_id,
    quantity: order.quantity,
    subtotal: order.subtotal,
    tax: order.tax,
    total: order.total,
  }
end
